#include <iostream>

using namespace std;

int main()
{

    /*
    Quicksort - 
    pciks an arbitrary value (a) and will cut the solution set in half.
    will use (a) as a pivot and sort each number less than (a)
    on the left of (a) and vice versa.
    will then do this for each half until solution set is sorted
    
    takes one space steps and scans for values that should go to the right or left partition


    for (int x = 0; x < arrayLen; x++)
    {   
        //x becomes our pivot
        int x = 0
        // base case: if there are 1 or zero elements to sort, parition is already sorted
        int smaller; //smaller saved value --- i will check for values that are greater than the pivot x
        int larger; //larger saved value --- j will bring that value to the right side
        if(smaller >= larger){
            return; 
        }
        //partition data within the array
        pivotLovation = partition(arr, smllaer, larger); // returns pivot loaciton
        cout << endl << enld;
        quickSort
        if (array[i] < array[i + (rand() % arrayLen + 1)])
        {
            larger = array
        }
    }
    */

}